{{- /*
Credentials are never rendered by this chart. Create a Secret out of band
(External Secrets Operator, SealedSecrets, ...) with keys `username` and
`password` and reference it via config.mqtt.existingSecret.

  kubectl create secret generic plantstream-mqtt \
    --from-literal=username=edge --from-literal=password=...
*/ -}}
