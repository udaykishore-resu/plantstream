apiVersion: v1
kind: Service
metadata:
  name: {{ include "plantstream.fullname" . }}
  labels:
    {{- include "plantstream.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plantstream.selectorLabels" . | nindent 4 }}
